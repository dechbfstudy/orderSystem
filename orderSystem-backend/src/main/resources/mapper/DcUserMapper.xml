<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dianchong.ordersystem.mapper.DcUserMapper">
    <select id="queryAllUsers" resultType="com.dianchong.ordersystem.entity.DcUser">
        select * from dc_user
    </select>

    <select id="queryByAccount" resultType="com.dianchong.ordersystem.entity.DcUser">
        select * from dc_user
        <where>
            USER_ACCOUNT = #{account}
        </where>
    </select>

    <update id="updateLoginInfo" parameterType="java.lang.String">
        UPDATE dc_user SET
            last_login_time = NOW(),
            login_count = COALESCE(login_count, 0) + 1
        WHERE USER_ACCOUNT = #{account}
    </update>
</mapper>