spring:
  application:
    name: orderSystem
  datasource:
    url: jdbc:mysql://localhost:3306/ORDER_SYSTEM_DB?useSSL=false&serverTimezone=UTC
    username: root
    password: dianchong888
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      schema-locations: classpath:initDB/create_db_data.sql
      mode: always

jwt:
  secret: dianchong888666
  expiration: 3600

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.dianchong.ordersystem.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
    cache-enabled: false
    lazy-loading-enabled: false
    aggressive-lazy-loading: true